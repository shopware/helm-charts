{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: fastlyngwafassociations.fastly.shopware.com
spec:
  group: fastly.shopware.com
  names:
    kind: FastlyNGWAFAssociation
    listKind: FastlyNGWAFAssociationList
    plural: fastlyngwafassociations
    shortNames:
      - fastlyngwafassoc
      - ngwafassoc
    singular: fastlyngwafassociation
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.serviceName
          name: Service
          type: string
        - jsonPath: .spec.workspaceName
          name: Workspace
          type: string
        - jsonPath: .status.state
          name: State
          type: string
        - jsonPath: .status.serviceID
          name: ServiceID
          type: string
        - jsonPath: .status.workspaceID
          name: WorkspaceID
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                serviceName:
                  minLength: 1
                  type: string
                workspaceName:
                  minLength: 1
                  type: string
              required:
                - serviceName
                - workspaceName
              type: object
            status:
              properties:
                message:
                  type: string
                serviceID:
                  type: string
                state:
                  type: string
                workspaceID:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
{{- end }}
