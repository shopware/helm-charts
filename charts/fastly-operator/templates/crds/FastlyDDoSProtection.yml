{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: fastlyddosprotections.fastly.shopware.com
spec:
  group: fastly.shopware.com
  names:
    kind: FastlyDDoSProtection
    listKind: FastlyDDoSProtectionList
    plural: fastlyddosprotections
    shortNames:
      - fastlyddosprotection
      - ddosprotection
    singular: fastlyddosprotection
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.serviceName
          name: Service
          type: string
        - jsonPath: .spec.mode
          name: Desired
          type: string
        - jsonPath: .status.currentMode
          name: Current
          type: string
        - jsonPath: .status.state
          name: State
          type: string
        - jsonPath: .status.serviceID
          name: ServiceID
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                mode:
                  enum:
                    - "off"
                    - log
                    - block
                  type: string
                serviceName:
                  minLength: 1
                  type: string
              required:
                - mode
                - serviceName
              type: object
            status:
              properties:
                currentMode:
                  type: string
                message:
                  type: string
                serviceID:
                  type: string
                state:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
{{- end }}
