{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: fastlypurgerequests.fastly.shopware.com
spec:
  group: fastly.shopware.com
  names:
    kind: FastlyPurgeRequest
    listKind: FastlyPurgeRequestList
    plural: fastlypurgerequests
    shortNames:
      - fastlypurge
      - purgerequest
    singular: fastlypurgerequest
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.state
          name: State
          type: string
        - jsonPath: .status.requestType
          name: Type
          type: string
        - jsonPath: .status.purgeCount
          name: Count
          type: integer
        - jsonPath: .spec.softPurge
          name: Soft
          type: boolean
        - jsonPath: .status.resolvedServiceID
          name: ServiceID
          type: string
        - jsonPath: .status.purgeID
          name: PurgeID
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                request:
                  properties:
                    purge:
                      properties:
                        serviceRef:
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                            - name
                          type: object
                        url:
                          format: uri
                          minLength: 1
                          type: string
                      required:
                        - serviceRef
                        - url
                      type: object
                    purgeAll:
                      properties:
                        serviceRef:
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                            - name
                          type: object
                      required:
                        - serviceRef
                      type: object
                    purgeKeys:
                      properties:
                        keys:
                          items:
                            type: string
                          minItems: 1
                          type: array
                        serviceRef:
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                            - name
                          type: object
                      required:
                        - keys
                        - serviceRef
                      type: object
                  type: object
                  x-kubernetes-validations:
                    - message: exactly one of purge, purgeKeys or purgeAll must be set
                      rule: '(has(self.purge) ? 1 : 0) + (has(self.purgeKeys) ? 1 : 0) + (has(self.purgeAll) ? 1 : 0) == 1'
                softPurge:
                  default: false
                  type: boolean
              required:
                - request
              type: object
              x-kubernetes-validations:
                - message: softPurge is not supported for purgeAll
                  rule: '!has(self.request.purgeAll) || self.softPurge == false'
            status:
              properties:
                completedAt:
                  format: date-time
                  type: string
                message:
                  type: string
                purgeCount:
                  format: int32
                  type: integer
                purgeID:
                  type: string
                purgeIDs:
                  additionalProperties:
                    type: string
                  type: object
                requestType:
                  type: string
                resolvedServiceID:
                  type: string
                state:
                  type: string
              type: object
          type: object
          x-kubernetes-validations:
            - message: spec is immutable
              rule: self.spec == oldSelf.spec
      served: true
      storage: true
      subresources:
        status: {}
{{- end }}
